---
label: 'Block: File download'
hide_body: true
display_field: button_text
fields:
- name: button_text
  type: text
  config:
    required: false
  label: Button text
- name: file_download
  type: file
  config:
    maxSize: 64
  label: File download
  description: Upload or select a file you would like visitors to download. If uploading
    a file try to rename it to something the visitor would understand after downloading
    e.g. small-groups.pdf rather than VXrdghSVr.pdf
