---
label: 'Block: Columns'
hide_body: true
display_field: display_name
fields:
- name: display_name
  type: text
  config:
    required: false
  label: Display name
  description: A name to identify the site section. This is to help you understand
    what the content is in the forestry editor.
- name: columns
  type: blocks
  label: 'Columns: Blocks'
  template_types:
  - solid-block
  - content-block
  - block-icon
  - image-text-block
  - top-image-text-block
  config:
    min: 1
    max: 6
- name: row_columns
  type: select
  default: Two
  config:
    required: false
    options:
    - Two
    - Three
    - Four
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Row columns
  description: How many columns should be displayed in a row?
