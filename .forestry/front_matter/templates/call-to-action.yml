---
label: Button
hide_body: false
display_field: button_text
fields:
- type: text
  name: button_text
  label: Button text
- type: select
  name: internal_link
  config:
    source:
      type: pages
      section: jekyll-pages
  label: Internal link
  description: Select a page that this content block should link to
- name: video_link
  type: boolean
  label: Video link
  description: Is this a video link? The video will play in a lightbox and display
    the video icon next to the button.
- name: external_link
  type: boolean
  label: External link
  description: Is this URL an external site, if so turn this on as the page will open
    in a new window.
